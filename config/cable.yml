development:
  adapter: async
  adapter: redis

test:
  adapter: async

#production:
 # adapter: redis
 # adapter: redis
 # url: <%= ENV.fetch("REDIS_URL") { "redis://localhost:6379/1" } %>
 # channel_prefix: bandra_production
